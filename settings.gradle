pluginManagement {
  repositories {
    maven {
      url 'https://repox.jfrog.io/repox/plugins.gradle.org/'
    }
    gradlePluginPortal()
  }
}

rootProject.name = 'sonar-auth-saml'

include 'sonar-auth-saml-plugin'

include 'tests'
